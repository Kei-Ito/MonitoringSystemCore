┌─────────────┐
│   App.vue   │ アプリケーション起動
└──────┬──────┘
       │
       ↓ store.initialize()
┌─────────────┐
│   Store     │ 初期化の entry point & データ管理
└──────┬──────┘
       │
       ↓ service.fetchXxx()
┌─────────────┐
│   Service   │ API呼び出し & エラーハンドリング
└──────┬──────┘
       │
       ↓ handleApiRequest
┌─────────────┐
│   API Layer │ 純粋な通信処理
└──────┬──────┘
       │
       ↓ データを返す
┌─────────────┐
│   Store     │ result.value で自身を更新
└─────────────┘